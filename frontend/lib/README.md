# Finance logic — yield & insurance math

Off-chain calculations for:

- **Yield** adjustments based on activity index (or attested values).
- **Insurance** payout eligibility and amounts (formulas aligned with on-chain logic where possible).

Deterministic and documented; used by the UI and optionally by API routes. No black-box models. Document formulas and sources (e.g. “matches contract X view Y”) in this README or in code comments.
