# Optional API keys. OSM/Overpass need no keys.
# Companies House (UK) — for employee data
# COMPANIES_HOUSE_API_KEY=

# Mapbox — optional for geocoding
# MAPBOX_ACCESS_TOKEN=

# Google Places — optional for price_range (otherwise inferred from revenue_proxy)
# GOOGLE_PLACES_API_KEY=
