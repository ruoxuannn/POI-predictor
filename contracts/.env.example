# Deployer key (no 0x prefix). Keep secret; never commit .env.
PRIVATE_KEY=

# Optional: override RPC (default in deploy script is Flare Coston2)
# RPC_URL=https://coston2-api.flare.network/ext/C/rpc
