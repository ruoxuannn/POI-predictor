# Solidity contracts

- Consume attestations from the Flare Data Connector (FDC).
- Implement **insurance payouts** and **yield adjustments** based on attested activity index.
- Expose view functions for frontend (e.g. get POI state, payout status, yield parameters).

Document contract roles and main entrypoints in this README as you add them.
