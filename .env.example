# Root .env.example — copy to .env and fill as needed.
# Each subfolder (data/, contracts/, frontend/) has its own .env.example; use those for local runs.

# -----------------------------------------------------------------------------
# Shared (optional)
# -----------------------------------------------------------------------------
# FLARE_RPC_URL=https://flare-api.flare.network/ext/bc/C/rpc
# FLARE_CHAIN_ID=14

# -----------------------------------------------------------------------------
# Data layer — see data/.env.example
# -----------------------------------------------------------------------------
# Contracts / frontend — see contracts/.env.example and frontend/.env.example
# -----------------------------------------------------------------------------
